const {DefaultHandler} = require("./handlers/default");
const {Brie} = require("./handlers/brie");
const {BackstagePasses} = require("./handlers/backstage_passes");
const {Conjured} = require("./handlers/conjured");
const {Sulfuras} = require("./handlers/sulfuras");

module.exports = {
    DefaultHandler,
    Brie,
    BackstagePasses,
    Conjured,
    Sulfuras
}
